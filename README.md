# phishing-detection
This project involves creating a web application that uses AI to detect phishing attempts in links and email content. It leverages pre-trained NLP models like DistilBERT, RoBERTa, and T5 for text classification. The application compares model performance based on metrics like precision, recall, and F1-score.
